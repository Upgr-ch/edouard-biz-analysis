kind = "web"
previewPath = "/"
title = "UpGrade – Édouard"
version = "1.0.0"
id = "artifacts/upgrade-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21133

[services.development]
run = "pnpm --filter @workspace/upgrade-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/upgrade-app", "run", "build" ]
publicDir = "artifacts/upgrade-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21133"
BASE_PATH = "/"
