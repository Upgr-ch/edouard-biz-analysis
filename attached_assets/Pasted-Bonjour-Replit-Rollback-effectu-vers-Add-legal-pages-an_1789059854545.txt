Bonjour Replit,

Rollback effectué vers "Add legal pages and supporting components" (fin de la Phase 4).

**Passons à la Phase 5 – Tentative 3, avec la correction suivante :**

**Correction principale : définir `PORT` avant tout build Vite lancé hors workflow.**

Pour éviter l'erreur `PORT environment variable is required but was not provided`, tu dois :

1. Définir explicitement `PORT=3000` (ou une valeur libre) dans l'environnement du script `build-production.mjs` et de tout script qui lance Vite hors workflow.
2. Vérifier que cette variable n'interfère pas avec le workflow normal (qui a son propre PORT).

**Rappel des corrections déjà validées pour cette tentative :**

1. **Build non-destructif** :
   - Construire dans `dist-tmp/`.
   - Valider tous les contrôles sur `dist-tmp/`.
   - Remplacer `dist/public/` uniquement si tout passe.
   - Si un contrôle échoue, ne rien toucher dans `dist/public/`.

2. **Healthcheck corrigé** :
   - Attendre que le port API soit disponible.
   - Utiliser `/api/healthz` (pas `/api`).
   - Ne pas considérer les 500 transitoires comme un échec.

3. **Test explicite du routage** (URL → HTML attendu → HTML obtenu) :
   - `/` → `index.html` (Vike)
   - `/cgu` → `cgu/index.html` (Vike)
   - `/cgv` → `cgv/index.html` (Vike)
   - `/mentions-legales` → `mentions-legales/index.html` (Vike)
   - `/confidentialite` → `confidentialite/index.html` (Vike)
   - `/cookies` → `cookies/index.html` (Vike)
   - `/auth` → `legacy-spa.html`
   - `/auth/sign-up` → `legacy-spa.html`
   - `/admin` → `legacy-spa.html`
   - `/route-inconnue` → `legacy-spa.html`

   **Si une page Vike retourne `legacy-spa.html` au lieu de son propre HTML, STOP.**

4. **Correction du title persistant** (régression Vike).

5. **Ajout du lien `/cookies`** dans `HydratablePublicApp`.

**Contraintes :**
- Aucune publication en production.
- Aucun push, aucun commit.
- Si un test échoue, rollback immédiat vers le checkpoint actuel (fin Phase 4).
- Zéro changement visible pour l'utilisateur.

**Livrables attendus :**

1. Les fichiers créés/modifiés.
2. Le résultat du build complet.
3. Le tableau de routage (URL → HTML attendu → HTML obtenu → statut).
4. Les tests fonctionnels (chat, Clerk, tracking, CLS).
5. Le contrôle Git.
6. Le statut des workflows.

**Ne pas publier. Ne pas pousser. Attendre ma validation.**

Merci.