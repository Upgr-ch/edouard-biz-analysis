Bonjour Replit,

Rapport de diagnostic validé. C'était un faux positif (healthcheck `/api` appelé trop tôt, alors que `/api/healthz` répond correctement). Aucun bug dans le code de la Phase 5.

**Mais je valide aussi le risque identifié** : le fallback `/* → legacy-spa.html` doit être testé explicitement pour ne pas intercepter les pages Vike.

**Relançons la Phase 5 avec les corrections suivantes :**

**Corrections obligatoires :**

1. **Build non-destructif** :
   - Construire Vike dans `dist-vike/`
   - Construire la SPA historique dans `dist-spa/`
   - Assembler dans un dossier temporaire `dist-tmp/`
   - **Valider TOUS les contrôles** sur `dist-tmp/`
   - **Seulement si tout passe**, remplacer `dist/public/` par `dist-tmp/`
   - Si un contrôle échoue, ne rien toucher dans `dist/public/`

2. **Healthcheck corrigé** :
   - Attendre que le port API soit disponible
   - Utiliser `/api/healthz` (pas `/api`)
   - Ne pas considérer les 500 transitoires comme un échec du build

3. **Test explicite du routage** :
   Pour chaque URL, vérifier que le HTML retourné est bien le bon :
   - `/` → `dist/public/index.html` (Vike accueil)
   - `/cgu` → `dist/public/cgu/index.html` (Vike CGU)
   - `/cgv` → `dist/public/cgv/index.html` (Vike CGV)
   - `/mentions-legales` → `dist/public/mentions-legales/index.html` (Vike)
   - `/confidentialite` → `dist/public/confidentialite/index.html` (Vike)
   - `/cookies` → `dist/public/cookies/index.html` (Vike)
   - `/auth` → `dist/public/legacy-spa.html` (fallback SPA)
   - `/auth/sign-up` → `dist/public/legacy-spa.html` (fallback SPA)
   - `/admin` → `dist/public/legacy-spa.html` (fallback SPA)
   - `/route-inconnue` → `dist/public/legacy-spa.html` (fallback SPA)

   **Si une page Vike retourne `legacy-spa.html` au lieu de son propre HTML, STOP et signale-le.**

4. **Correction du title persistant** (régression Vike) : à traiter pendant cette tentative.

5. **Ajout du lien `/cookies`** dans `HydratablePublicApp` : à traiter pendant cette tentative.

**Contraintes :**
- **Aucune publication en production.**
- **Aucun push, aucun commit.**
- **Si un test échoue, rollback immédiat vers le checkpoint actuel (fin Phase 4).**
- Zéro changement visible pour l'utilisateur.

**Livrables attendus :**

1. Les scripts corrigés (`publish-static-output.mjs` non-destructif).
2. Le résultat du build complet.
3. Le tableau de routage testé (URL → HTML attendu → HTML obtenu → statut).
4. Les tests fonctionnels (chat, Clerk, tracking, CLS).
5. Le contrôle Git.
6. Le statut des workflows.

**Ne pas publier. Ne pas pousser. Attendre ma validation.**

Merci.