Bonjour Replit,

Le plan de la Phase 5 est **validé intégralement**. Il est précis, complet, et prévoit un rollback clair.

**Confirmations :**

1. **Stratégie de build** : `publish-static-output.mjs` validé.
2. **Fallback SPA** : `legacy-spa.html` validé. La modification de `artifact.toml` doit être faite par la procédure Replit validée, pas manuellement.
3. **Fichiers publics** : la vérification stricte est validée. Le build doit échouer si un fichier manque.
4. **Compatibilité build historique** : les hashes doivent rester identiques.
5. **Correction title persistant** : validée. Corrige-le pendant la Phase 5.
6. **Ajout du lien `/cookies`** : validé. Un cinquième lien dans le footer, aucun autre changement.
7. **Baseline avant modif** : validée. Fais le relevé complet avant de toucher au build.
8. **Vérifications post-bascule** : validées. Toutes doivent passer avant tout push.
9. **Rollback** : validé. Si un test échoue, rollback immédiat vers le checkpoint précédent.
10. **Production non touchée** : validé. Aucune publication pendant cette phase.

**Contraintes rappelées :**
- Zéro changement visible pour l'utilisateur.
- Le chat doit rester strictement identique.
- Clerk, tracking, tunnel : inchangés côté utilisateur.
- Eugène : non touché.
- Aucun push, aucun commit sans validation explicite.
- La production Replit ne doit pas être publiée tant que je n'ai pas validé.

**Prochaine étape : démarre la Phase 5.**

Déroule dans l'ordre :
1. Baseline complète (tests avant modif).
2. Création de `publish-static-output.mjs` et `build-production.mjs`.
3. Modification du `package.json` (scripts).
4. Modification de `artifact.toml` via la procédure Replit (fallback vers `legacy-spa.html`).
5. Correction du title persistant.
6. Ajout du lien `/cookies` dans `HydratablePublicApp`.
7. Build complet en local.
8. Tous les tests prévus (structure, pages Vike, fallback SPA, fonctionnalités, contrôles statiques).
9. Rapport complet.

**Ne pas pusher. Ne pas publier en production.**

Présente-moi :
- Le résultat de la baseline.
- Les fichiers créés/modifiés.
- Le résultat du build complet.
- Le résultat de chaque test.
- Le contrôle Git.
- Le statut des workflows.

Merci.